{
    
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Principal",
            "type": "python",
            "request": "launch",
            "program": "${main.py}",
            "console": "integratedTerminal",
            "args": "${command:pickArgs}"
        }
    ]
}